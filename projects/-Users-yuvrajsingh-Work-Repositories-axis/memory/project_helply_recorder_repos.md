---
name: helply-recorder-repos
description: "New helply-recorder-extension and helply-recorder-app repos (rebranded from instantdocs), their axis API contract, R2 update bucket, and remaining blockers"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8740c10e-4dbf-4535-82ac-5dcbcef87f9b
  modified: 2026-09-17T07:57:47.847Z
---

Created 2026-09-15 from the instantdocs recorder repos (kept intact next to them in Work/Repositories): private repos GrooveHQ/helply-recorder-extension and GrooveHQ/helply-recorder-app, both v1.0.0, fresh git history, builds verified.

Stripped everywhere: Supabase (progress now via chrome.runtime port / IPC), BetterStack, Sentry, credits/plan billing. Clicks payload simplified to `{x, y, t}` (0..1 fractions of the recorded surface, t = ms from recording start).

Phase 2 SHIPPED to PR: axis PR #1436 (`ys/feat/recorder-backend`, pushed; commits incl. `bc56ddc49` quality refactor). Post-review state: clicks cross the wire as a real JSON array validated by nested DTOs (no more stringified-JSON-in-JSON, `parseClicks` deleted); recording ids generated app-side with `uuid` v7 (attachment.service pattern, single flush); wire `RecorderVideoMultipart` = `{partSize, partUrls}` only (key/uploadId stay server-side); `mintSessionToken` + `toWebHeaders` live canonically in `apps/backend/src/auth/*.util.ts` (CLI controller reuses them; four older toWebHeaders copies in guard/handler/kb-authorize/bull-board remain as follow-up debt). react-doctor diff gate: 100/100. Icons done in both clients (rsvg-convert/iconutil/magick from ~/Downloads/helply-logo.svg). Publish creds wired: `npm run package:publish` = `op run --env-file=.env.publish` reading `op://Axis Dev/helply-recorder-r2/*`.

Phase 2 module shape (axis branch `ys/feat/recorder-backend`): `modules/recorder` facade `/api/recorder/*` — session/me/workspaces (BA listOrganizations, incl slug)/knowledge-bases (KnowledgeBaseService export)/recordings (create+presign)/:id/audio-upload-url (sized presign AFTER client audio extraction)/:id/complete-multipart/:id/status/:id. `RecorderRecording` entity + Migration20260914204545 (hand-trimmed: the committed MikroORM snapshot on master was STALE — regen re-emitted ticket_forms etc; kept the corrected snapshot). Org scoping via `x-axis-org-id` header. Desktop auth: `/api/recorder/auth/callback` mints BA session token (prod-enabled CLI-flow mirror) → deep link `helply-recorder://auth?sessionToken=`, app sends Authorization Bearer (csrf/cookie machinery deleted). Web: `features/recorder` polling status page at `/{workspaceSlug}/recordings/{id}` — both clients open it post-upload. Wiki: `wiki/pages/feature-recorder.md`.

URLs (real): app https://next.helply.com, api https://next.helply.com/api (same origin). Extension ID pinned via manifest `key` → `chrome-extension://kgijlpfmjhnlcpgmbhpmkhkleijelklg` (private key at ~/Downloads/helply-recorder-extension-key.pem — must go to 1Password); that origin must be added to `CORS_ALLOWED_ORIGINS` + `BETTER_AUTH_TRUSTED_ORIGINS` on deployed backends. Recordings buckets: private R2 `helply-recordings{,-staging}`. Vault creds DONE (Jared, 2026-09-15): item `cloudflare` (lowercase), section `KB Recordings`, fields `s3_endpoint`/`access_key_id`/`secret_access_key` in both Axis Production and Axis Staging; committed `.env.production`/`.env.staging` refs point there (bucket names stay literal).

Machine gotchas hit: taskforce npm token lives in vault field `npm_taskforcesh_token`-ish on the Taskforce item (the `token` field is a placeholder); pnpm/mise/global-npmrc were lost in a machine cleanup and reinstalled.

Auto-update: R2 bucket `helply-recorder-apps` on the Sharedservices CF account (`CLOUDFLARE_ACCOUNT_ID=65e296b71eaf0e794c6e4f90e0f6f27c` — must export it, login has two accounts). Public feed https://pub-435ede51c76b45338f90b1dcf3d79bf6.r2.dev (r2.dev enabled; custom domain later). electron-builder publish = [generic feed, s3 upload via the account endpoint]; publishing needs an R2 API token as AWS_* env.

Never touch `assets/scripts/makewindowtransparent.node` (original dev: Windows recorder dies). Root cause of its phantom git diff was `* text eol=lf` in .gitattributes with no `*.node binary`; fixed in the new repo. Old repo still has the bug.

Windows testing DONE (2026-09-17) in a Fusion Win11 ARM VM (sandbox stays on the Mac — no nested virt, Docker can't run in the VM). Plumbing: portless proxy needs `--lan` (loopback-only by default); Chromium hardcodes `*.localhost`→127.0.0.1 ignoring hosts, so the VM uses `netsh interface portproxy` 127.0.0.1:443→MacIP:443 AND :9000→:9000 (MinIO presigned PUTs); portless CA via `certutil -addstore -f Root` in an ADMIN shell. Cross-build installers from the Mac: `npm run build && npx electron-builder build --win` (arm64 on this Mac; add `--x64 --arm64` for releases). App PR #3 (merged, dc2d8ca): Windows clicks + GetWindowRect bounds are PHYSICAL px vs Electron DIPs — normalized via screen.screenToDipPoint/screenToDipRect; before the fix, fractions were scaled by the display factor and lower-right-half clicks silently dropped. OPEN, needs real-x64-hardware measurement before release: Windows video lags click timestamps ~1.4s in the VM (MediaRecorder onstart fires before first captured frame; `recordingStartTime = onstart − magic 300ms` in App.tsx; mac gap ≈0 — do NOT retune the anchor blindly); phase 3 frame extraction should scan forward from t for a settled frame, which also yields better article screenshots. makewindowtransparent.node loading is commented out in main.ts (x64-only binary; revisit for the win x64 release).

Local E2E testing DONE (2026-09-16): both clients verified end to end on the sandbox (extension multipart path + desktop single-PUT path), clicks frame-verified precise. Shipped from it: app PR #1 (CSP `http://localhost:*` for sandbox MinIO PUTs; drop clicks on own windows; electron allowScripts) and PR #2 (permission flow: no double Settings dialog, mic via askForMediaAccess in main as single source of truth, isOnOwnWindow checks interactive windows only — highlight/confetti overlays are click-through and were swallowing content clicks; polls collapsed to pollUntilGranted). Axis `3c3c42602` on PR #1436: auth callback 302s to `${webURL}/sign-in?redirect=/api/recorder/auth/callback` when sessionless. Gotchas: deep links need the PACKAGED app (Info.plist scheme; dev is a no-op on mac); `package:mac-unsigned` output has a broken linker-signed stub — must `codesign --force --deep --sign - --identifier com.helply.recorder` or TCC grants never stick; each rebuild needs `tccutil reset ScreenCapture|Microphone|Accessibility com.helply.recorder` re-grant until real signing certs. electron's install.js extract-zip can silently produce a 260K dist — extract the cached zip with ditto. extension `.env` + `.env.example` carry sandbox URLs active (`https://inbox.helply.localhost` / `https://api.inbox.helply.localhost/api`) with prod pair commented; app `src/shared/config.ts` prod active with sandbox pair commented. `apps/backend/.env.local` has the extension origin appended to CORS_ALLOWED_ORIGINS + BETTER_AUTH_TRUSTED_ORIGINS. Sandbox boot must be run by the USER in their terminal (`bin/sandbox`) — `OP_SERVICE_ACCOUNT_TOKEN` is missing from ~/.config/mise/config.toml so op run steps need interactive 1Password. Load unpacked extension from its dist/; login your-email@example.com/password; verify MinIO object via `docker exec axis-minio ... mc ls -r local/axis-attachments/recordings/`.

Open items: (1) DONE — recordings R2 vault fields added by Jared, env refs updated (uncommitted on the branch as of 2026-09-15); (2) watch PR #1436 CI (migration job validates the trimmed migration; Sonar may flag); (3) at deploy add extension origin to CORS/trusted origins on Railway; (4) extension signing key pem — user vaulted it (done), local Downloads copy should be deleted; (5) code signing certs + Chrome Web Store before public release; (6) later: downloads.helply.com custom domain on helply-recorder-apps (DECIDED against folding update hosting into helply-production — R2 tokens scope per-bucket, a publish token on the prod bucket could overwrite customer data); (7) follow-up PR: consolidate 4 remaining toWebHeaders copies; (8) recording retention/lifecycle decided in phase 3; (9) phase 3 = recording → step-by-step KB article generation, replaces the status page.
